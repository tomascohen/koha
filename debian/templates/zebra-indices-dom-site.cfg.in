# Zebra configuration file for Alvis XSLT filter setup including
# SRU/SRW Webservice setup and CQL-to-PQF configuration
#
#

# profilePath - where to look for config files
profilePath:__ZEBRA_CONF_DIR__/indices:__ZEBRA_CONF_DIR__/lang_defs/en:__ZEBRA_CONF_DIR__/etc

# modulePath - where to look for loadable zebra modules
modulePath: /usr/lib/idzebra-2.0/modules

encoding: UTF-8


# Files that describe the attribute sets supported.
attset: bib1.att


# store records and record keys internally
storeData: 1
storeKeys: 1

# use the DOM XML filter with config file dom-conf.xml
recordtype: dom.__ZEBRA_CONF_DIR__/indices/dom-conf.xml
#recordId: (bib1,indice)


# where to put registers, and other var content, and how large they may be
# Lock File Area
lockDir: __ZEBRA_LOCK_DIR__/indices
perm.anonymous:ar
perm.kohauser:rw
passwd: __ZEBRA_CONF_DIR__/etc/passwd
register: __ZEBRA_DATA_DIR__/indices/register:20G
shadow: __ZEBRA_DATA_DIR__/indices/shadow:4G

# Temp File area for result sets
setTmpDir: __ZEBRA_DATA_DIR__/indices/tmp

# Temp File area for index program
keyTmpDir: __ZEBRA_DATA_DIR__/indices/key


# enable pre-sorting of hit lists according to static rank
#staticrank: 1

# use rank sorting filter
#rank: rank1
